# tasodepo1
#include <stdio.h>
int main(){
  int a,b,ebob,i;
  printf("iki tam sayı giriniz: ");
  scanf("%d%d",&a,&b);
  for(i=1; i<=a; i++)
  {
    if(a%i==0&&b%i==0)
    {
      ebob=i;
    }
  };
    printf("%d\n",ebob);
}
